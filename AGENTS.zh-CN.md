# 仓库开发规范

## 工作范围与流程

本仓库是 Go MCP Server 模块：`github.com/wuxujun/xktmcp`。以满足任务为前提进行最小、安全的修改。除非用户明确要求，否则不要重构无关代码、升级依赖、重命名公开 API，或修改配置默认值。

修改前，先确认目标行为、相关包和定向验证命令。优先检查任务明确提到的文件；如确实需要扩大范围，先简要说明原因，并只检查最少的相关文件。

对于非简单任务，按以下顺序执行：

1. 检查相关实现和测试。
2. 简要说明根因和最小修改计划。
3. 仅修改必要文件。
4. 格式化变更文件并运行定向测试。
5. 汇报变更文件、验证结果和剩余风险。

当需求冲突、改动会影响公开行为、需要数据库迁移，或定向测试已失败两次时，不要继续扩大修改范围；停止并请求确认或报告下一步所需信息。

## 项目结构

入口文件为 `cmd/server/main.go`，负责组装传输层、认证、日志和优雅退出。

核心代码位于 `internal/`：

- `client`：上游 HTTP 客户端和重试辅助逻辑
- `service`：参数校验和业务编排
- `tools`：MCP 工具及其 Schema
- `model`：DTO 数据结构
- `auth`、`logger`、`metrics`、`pii`、`trace`：通用支撑包

测试文件与源代码同目录，命名为 `*_test.go`。

## 上下文与工具限制

保持上下文小且与任务直接相关。

- 只读取与任务相关的文件，优先读取用户明确指定的文件。
- 在给出计划前，最多检查 5 个文件；任务明显需要更多文件时除外。
- 单次文件读取最多 250 行；使用指定行范围、`rg -n` 或符号搜索定位内容。
- 搜索结果最多保留 20 条。先在具体目录内搜索，再考虑仓库根目录。
- 不要重复读取未变化的文件，也不要重复执行结果不会变化的命令。
- 命令输出最多保留 120 行相关内容；先通过 `rg`、`head`、`tail` 或测试/包筛选进行过滤。
- 除非任务明确需要，否则不要读取 `.git/`、`vendor/`、`node_modules/`、`dist/`、`build/`、`coverage/`、生成产物或历史日志目录。
- 不要输出密钥、`.env` 内容、Token、凭据或完整生产日志。
- 最多执行两轮“实施修改并重新测试”。若仍然失败，停止修改，报告已有证据、当前假设和建议的下一步诊断动作。

优先使用定向命令。除非用户要求或确有必要完成最终验证，否则不要执行全仓库测试、全量 lint、Docker 重建、数据库迁移或依赖网络的命令。

## 构建、运行与测试

- `go run ./cmd/server/main.go`：使用默认 stdio 传输启动服务。
- `go run ./cmd/server/main.go -transport=http -port=8081`：通过 `/mcp` 启动 Streamable HTTP；仅调试时添加 `-debug`。
- `go run ./cmd/server/main.go -transport=sse -port=8081`：启动 SSE 端点。
- `go test ./internal/<package> -run TestName -v`：调试时运行指定包中的单个测试。
- `go test ./internal/<package>`：运行目标包的测试套件。
- `go test ./...`：运行完整测试套件；在提交 PR 前执行，或仅在任务明确要求全量验证时执行。
- `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -tags=jsoniter -ldflags="-s -w" -o mcp-server ./cmd/server/main.go`：构建 Linux 发布二进制文件。

修改 Go 文件后，仅对已修改的 `.go` 文件执行 `gofmt -w`。优先运行最窄范围的相关测试。除非任务要求运行时验证，否则不要启动长期运行的服务。

## Go 编码规范

使用惯用 Go 写法和标准格式化。

- 包名保持简短且使用小写。
- 仅导出跨包真正需要的 API；`internal` 包内优先使用未导出辅助函数。
- 遵循现有命名模式：`StudentAPI`、`RagAPI`、`*Service`、`*Tool`、`*Handler`。
- 除非明确要求破坏性变更，否则保持现有公开请求和响应结构不变。
- 返回错误时提供有意义的操作上下文；需要保留错误链时使用 `%w`。
- I/O 调用链应传递 `context.Context`。
- 使用 `internal/logger` 中的结构化日志辅助函数；不要新增直接调用 `log.Printf` 的代码。
- 标准库或现有依赖可以解决时，不新增依赖。

## 测试规范

使用 Go 标准 `testing` 包，测试放在被测代码旁边。

- 行为发生变化时，必须新增或更新定向测试。
- 参数校验、认证、Schema 和错误分支优先使用表驱动测试。
- 涉及认证、Schema 校验、PII 脱敏、缓存、指标或客户端错误处理时，覆盖相应的已变更行为。
- 测试可观察行为，而不是私有实现细节。
- 提交 PR 前在可行情况下运行 `go test ./...`；如果未运行，明确报告实际执行的定向测试及未运行全量测试的原因。

## 安全与配置

不得提交或暴露 `.env` 文件、API Token、凭据、生成日志或 PII。

运行时配置可能包括 `API_TOKEN`、`BASE_URL`、`AUTH_TOKEN`、`AUTH_REMOTE_VERIFY_URL`、`AUTH_IP_ALLOWLIST` 和 `RAG_SEMANTIC_REWRITE`。

认证、远程校验、IP 白名单、PII 脱敏、`/metrics`、`/health`、出站 HTTP 行为和日志相关的改动均属于安全敏感变更。任何会改变这些行为的修改，先说明部署或安全影响。必要时通过部署层网络控制保护 `/metrics` 和 `/health`。

## 提交与最终汇报

Commit 保持简洁、面向动作，并且一个提交只处理一个关注点。当前仓库常用简短中文编号摘要，例如：`1. 优化 logger 路径解析逻辑`。

PR 应说明行为变更、已运行的定向测试、配置或安全影响以及剩余风险。仅在用户可见输出发生变化时附截图。

最终回复只包含：

1. 变更文件
2. 修改内容
3. 验证命令与结果
4. 配置、安全或兼容性影响
5. 剩余风险或下一步建议

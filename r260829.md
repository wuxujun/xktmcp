# xktmcp 项目检查总结报告

检查日期：2026-08-29  
检查范围：项目结构、入口与核心调用链、认证与安全、HTTP Client、MCP Tools、Service、本地 Wiki、测试、文档及当前工作区状态。

后续修复状态（截至 2026-09-01）：报告中的主要安全与正确性问题已完成闭环，并已推送至 `origin/main`。最近提交包括：RAG 参数契约、本地 Wiki 独立运行、租户配置校验、CI 漏洞扫描、版本注入、就绪探针、工具/Prompt 白名单及客户端代理统一。

## 一、总体结论

当前项目已经具备较完整的 MCP 服务工程形态，分层清晰，包含 11 个 MCP 工具，支持 stdio、SSE、Streamable HTTP、认证、多租户 ACL、缓存、熔断、审计日志、Prometheus 指标，以及远程和本地 Wiki 后端。

现阶段可评价为：**基础功能较完整、测试稳定，但身份信任边界和部分可靠性逻辑尚不适合直接作为强多租户隔离方案投入生产。**

本报告初版仅进行检查分析；后续已按优先级完成多项修复，详见下方“已完成修复”。

## 修复进展（截至 2026-09-01）

- 认证主体与请求参数身份冲突拒绝，防止跨租户 Wiki 路由伪造。
- MCP 请求体统一限制为 4 MiB，超限返回 413；ACL 解析失败拒绝请求。
- 远程认证缓存采用有界 TTL/LRU，支持 `AUTH_REMOTE_CACHE_MAX_ENTRIES`。
- 写请求默认不重试；带 `Idempotency-Key` 时支持请求体安全重放。
- RAG 实现 `top_k`、`min_score`、`include_sources`、`include_chunks` 参数语义。
- 本地 Wiki 可脱离上游 API 独立运行，并仅注册 Wiki 工具。
- 租户令牌哈希非法、重复或租户名称为空时启动失败。
- CI 增加 `govulncheck`，并支持 release 版本注入。
- 新增 `/ready` 就绪探针；工具与 Prompt 支持白名单注册。
- 统一上游客户端遵循系统代理，并在重试前排空有限响应体。
- README 中英文同步更新，忽略压缩日志文件。

## 二、当前能力

### 1. MCP 工具

- 学员：搜索、详情、订单、考试成绩。
- RAG：知识检索、规则查询改写、客户端 LLM Sampling 查询改写。
- 教职工：人员、组织、课程相关搜索。
- Wiki：搜索、页面详情、目录树、创建/覆盖/追加页面、反向链接。

### 2. Wiki 本地模式

- Markdown 文件索引与定时刷新。
- 页面原子写入和版本递增。
- 写入目录限制和符号链接逃逸防护。
- Markdown 链接及 `[[wiki link]]` 反向链接识别。
- 反向链接在索引刷新阶段预计算，成功写入并刷新后对查询可见；刷新失败保留上一版索引。
- 按显式 `userId` 配置路由到独立 Wiki 目录。
- `require_user_mapping` 严格用户映射模式。

### 3. 基础设施

- Bearer Token、远程认证、IP 白名单、多租户工具 ACL。
- TTL、LRU、后台清理组成的有界工具缓存。
- 学员、RAG、Staff、Wiki 模块独立熔断器。
- 上游请求超时、退避重试。
- PII 响应脱敏、审计日志、Trace ID。
- Prometheus 工具调用、缓存命中和熔断状态指标。
- 日志轮转、HTTP Payload 可控日志和优雅关闭。

## 三、验证结果

| 检查项 | 结果 |
| --- | --- |
| `go test ./...` | 全部通过 |
| `go test -race ./...` | 全部通过，未发现数据竞争 |
| `go vet ./...` | 通过 |
| 总语句覆盖率 | 约 62.5% |
| 编译 | 完成，出现非致命 Go 模块缓存写权限提示 |
| 漏洞数据库扫描 | 已加入 GitHub Actions `govulncheck` |

覆盖率较好的模块：

- `internal/auth`：82.9%。
- `internal/service`：84.0%。
- `internal/pii`：86.2%。
- `internal/wiki`：79.1%。
- `internal/trace`：92.3%。

覆盖率相对薄弱的模块：

- `cmd/server`：42.7%。
- `internal/client`：42.5%。
- `internal/tools`：44.4%。
- `internal/server`：无测试文件。

高风险逻辑恰好集中在 Client、Tools、Server 等低覆盖区域，例如 POST 重试、ACL 大请求体以及认证身份与 Wiki 用户路由绑定。

## 四、优先处理的问题

### P0：认证身份可以被工具参数覆盖（已解决）

`internal/trace/trace.go` 中 `EffectiveUserID` 当前采用以下优先级：

```text
工具参数 userId > 已认证 context 中的 userId
```

Wiki 用户路由直接使用该结果。因此，合法认证用户可以在请求体中填写另一个已经配置的 `userId`，从而访问该用户对应的 Wiki 目录。`AUTH_TENANTS` 也没有把租户身份注入为不可覆盖的主体。

这意味着当前的“按用户目录隔离”主要是路由隔离，并不是可靠的访问控制隔离。

建议：

- 将远程认证或租户认证得到的 principal 设为权威身份，禁止工具参数覆盖。
- 分离 `AuthenticatedPrincipal`、`RequestedUserID` 和 `AuditSubject`。
- 为租户配置增加固定 principal/user mapping。
- 增加跨租户伪造 `userId` 的端到端拒绝测试。

### P1：租户 ACL 会静默截断大请求（已解决）

`internal/auth/auth.go` 中 `extractToolName` 只读取 64 KiB，然后把读取到的部分作为完整 Body 重新交给 MCP：

- 超过 64 KiB 的 Wiki 正文会被截断。
- 超限时没有返回 413，也没有显式错误。
- JSON 解析失败时 `toolName` 为空，ACL 检查会跳过，属于提取层面的 fail-open。
- 使用全局静态 Token 时又没有统一的 MCP 请求体上限。

建议使用统一且可配置的 MCP Body 上限，例如 4 MiB，并在超限时明确返回 HTTP 413；ACL 解析失败时应拒绝请求，而不是继续执行。

### P1：远程认证缓存无容量上限和过期清理（已解决）

远程 Token 验证使用 `sync.Map` 缓存结果。过期条目不会主动删除，只会在相同 Token 再次出现时覆盖。

攻击者持续提交不同的无效 Token，可以导致缓存长期增长。虽然远程请求本身有限流，但长时间运行后仍可能积累大量条目。

建议复用项目已有的有界 TTL/LRU 缓存，或为认证缓存增加容量上限和定期清理任务。

### P1：POST 重试不安全（已解决）

`internal/client/client.go` 会复用同一个 `http.Request` 重试：

- Wiki Upsert 首次发送后，后续尝试可能使用已经消费的 Body。
- 写操作遇到 5xx 时自动重试，存在重复创建或重复追加风险。
- 当前没有幂等键。

建议：

- 每次尝试通过 `req.Clone` 和 `GetBody` 重建请求体。
- 默认只自动重试 GET 等幂等请求。
- 写操作应在引入 idempotency key 后，才对明确的暂态错误重试。
- 增加 POST Body 重放和“上游已经写入但返回 5xx”的测试。

### P1：RAG 参数尚未真正生效（已解决）

`RagSearchHandler` 计算了 `top_k` 和 `min_score`，但没有用它们过滤或截断返回结果：

- `top_k` 只出现在响应元数据和缓存键中。
- `min_score` 没有参与结果过滤。
- `include_sources=false` 时，`context` 中仍包含来源 URL。
- `include_chunks=false` 时，`context` 中仍包含全部切片正文。

这是明确的接口语义不一致，需要补齐实现和边界测试。

## 五、其他待优化问题

### 1. 认证与会话

- 无效的 `AUTH_REMOTE_VERIFY_URL` 仍会被视为“已配置认证”，可能导致服务启动后所有请求均认证失败。
- `sessionUserID` 提供了 `CleanSession`，但项目中没有调用，长期有状态会话可能残留映射。
- 租户 `token_hash` 缺少严格格式和重复配置校验。
- IP 白名单放行和全局 Token 模式缺少稳定的用户主体，无法安全支持严格的用户目录授权。

### 2. 服务可靠性

- `/health` 只表示进程存活，没有检查上游、Wiki 索引或必要配置状态。
- SSE/Streamable HTTP 为保证长连接没有设置整体 Read/Write Timeout，因此更需要统一 Body 限制、连接数限制和反向代理保护。
- 5xx 响应关闭 Body 前未进行有限排空，可能降低 HTTP 连接复用效率。
- Client 对系统代理的处理不一致：Student 使用默认 Transport，RAG、Staff、Wiki 显式禁用 Proxy。

### 3. 模块化与配置

- 所有工具统一初始化并强制要求 `API_TOKEN`；即使只运行本地 Wiki，也无法完全脱离上游配置。
- 工具无法按部署场景选择性启用，权限范围和故障域偏大。
- 熔断阈值、冷却时间、缓存 TTL 和容量多数为硬编码，缺少环境级调优能力。
- Server 版本固定为 `1.0.1`，没有注入 Git commit、构建时间等版本信息。

### 4. Wiki 后续扩展

- 本地 Wiki 搜索目前是标题、摘要、正文的简单包含和计数算法，不是 BM25、倒排索引或向量检索。
- 索引刷新会扫描全部 Markdown 文件，数据量增大后会产生锁等待和磁盘开销。
- 反向链接已改为在刷新时预计算并随索引快照发布，查询不再扫描全部文档；成功写入并刷新后新结果可见，刷新失败保留上一版结果。
- 严格用户映射模式仍会初始化默认 Wiki，并把默认文档计入日志中的总文档数，容易造成运维理解偏差。
- 写入缺少版本冲突控制，例如 `expected_version` 或 ETag，多个进程同时写同一目录时仍可能发生覆盖。

### 5. 文档与仓库治理

- README 中的 `-debug` 参数实际不存在。
- README 开头仍将项目描述为“学生相关数据接口服务”，没有完整说明现有 11 个工具和完整认证配置。
- `CLAUDE.md` 中的工具数量和部分依赖信息已经落后。
- `config/wiki.example.json` 应同步加入多用户路由示例或引用说明。
- 当前存在未跟踪压缩日志 `server-2026-08-21T00-36-52.229.log.gz`。
- `.gitignore` 只忽略 `*.log`，没有忽略 `*.log.gz`，存在日志及敏感数据误提交风险。
- 未发现 CI 配置，测试、竞态检查、静态检查和漏洞扫描尚未自动化。
- 覆盖率执行对无测试的 `internal/server` 报出 `no such tool "covdata"`，虽然生成了可用 profile，仍建议统一 CI Go 工具链。

## 六、当前未提交工作

检查时工作区包含以下现有改动：

- `README.md` 修改。
- `internal/server/register.go` 修改。
- `internal/wiki/config.go`、`config_test.go` 修改。
- 新增 `internal/wiki/local_router.go`、`local_router_test.go`。
- 未跟踪压缩日志 `server-2026-08-21T00-36-52.229.log.gz`。

这组 Wiki 改动的显式目录映射、严格模式、路径安全和基础单元测试整体合理，但在提交或生产部署前，应优先解决“认证身份可被请求参数覆盖”的问题，否则文档中的“用户独立目录”容易被误解为安全隔离。

## 七、建议实施顺序

### 第一阶段：安全和正确性

1. 修复认证身份信任边界，完成真正的租户/Wiki 用户授权绑定。
2. 统一请求体大小限制，修复 ACL 截断和 fail-open。
3. 将认证缓存改为有界 TTL/LRU，并接入会话清理。
4. 修复 POST 重试和写操作幂等性。

### 第二阶段：补齐已有功能契约

1. 落实 RAG 的 `top_k`、`min_score`、`include_sources` 和 `include_chunks`。
2. 增加就绪探针和关键依赖状态检查。
3. 支持按部署场景选择性注册工具。
4. 允许本地 Wiki 独立运行，不强制依赖上游 API Token。

### 第三阶段：质量保障

1. 为 Client、Tools 和 Server 增加集成测试。
2. 将总语句覆盖率提升到至少 75%。
3. 增加 CI：`go test`、`go test -race`、`go vet`、`govulncheck` 和 Linux release build。
4. 增加远程认证、租户 ACL、协议版本兼容和大请求体的端到端测试。

### 第四阶段：性能和产品化

1. 为 Wiki 引入增量索引、倒排索引/BM25 或可选向量检索。
2. 增加配置化缓存、熔断和限流参数。
3. 完善 README、配置参考、部署说明和监控告警示例。
4. 注入 Git commit 和构建时间。

## 八、最终评价

项目的工程基础较好，分层、测试、日志、缓存、熔断、脱敏和本地 Wiki 的路径安全实现均有明显积累；当前 Wiki 本地化和多目录路由也已经基本成型。

下一阶段不宜继续优先堆叠新工具，应先完成以下四项闭环：

1. 身份和授权边界。
2. 请求体及 ACL 安全边界。
3. 写操作重试与幂等性。
4. RAG 参数契约一致性。

完成上述工作后，再推进 Wiki 检索性能、模块化部署和可观测性增强，会更适合进入稳定生产阶段。

上述“优先处理的问题”保留为初版审计记录；当前仍待推进的重点主要是 Wiki 增量/倒排索引、配置化限流与熔断参数，以及 Client/Tools/Server 集成测试和覆盖率提升。
